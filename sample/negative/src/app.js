const latency = measure();
const emailInput = document.querySelector(".email-input");
const user = process.env.USERNAME;
const phone = "iPhone";
const mobile = window.innerWidth < 768;
const rrn = "release-run-number";
const ssn = require("./ssn-lib");
await prisma.messages.create({ data: {} });
const lat = 3; // unrelated
const price = "100원";
const id = "123456-1234567"; // not a valid RRN month
type Props = { email: string };
